# Recitation1

1) git add .
   adds files to commit, can replace . with file name to add specific file
2) git config --global user.name "Dongamet"
   authorizes (do once)
3) git commit -m "Message"
   saves the changes, doesnt publish to github yet
4) git push
   publishes the changes

